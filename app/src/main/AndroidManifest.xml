<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PRINCIPAL.frmregistrardocente"></activity>
        <activity android:name=".PRINCIPAL.frmresgistraralumno" />

        <service
            android:name=".CONTROLADOR.estudiantecontrolador"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".CONTROLADOR.personalcontrolador"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".PRINCIPAL.frmprincipalpersonal" />
        <activity android:name=".PRINCIPAL.frmprincipalestudiante" />
        <activity android:name=".PRINCIPAL.frmloginestudiante" />
        <activity android:name=".PRINCIPAL.frmloginadmin" />
        <activity android:name=".PRINCIPAL.index">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>